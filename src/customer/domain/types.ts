export enum CloudCustomerType {
  ALTA = "alta",
  MEDIA = "media",
  BAJA = "baja",
}

export enum CustomerType {
  REGULAR = "Regular",
  CLOUD = "Cloud",
}

export enum CustomerStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
